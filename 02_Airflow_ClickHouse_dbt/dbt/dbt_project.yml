name: ac_enery_usage
version: "1.0.0"
profile: ac_enery_usage  # must match the top key in profiles.yml
model-paths: ["models"]
target-path: "target"
clean-targets: ["target"]

models:
  ac_enery_usage:
    +materialized: table
    staging:
      +schema: staging  # Place staging models in a separate database (schema) "staging"
      +materialized: view
